# CP
All About My Competitive Programming :D

## Test script

Install the Python dependencies before running `Test.py`:

```bash
python3 -m pip install -r requirements.txt
```

Then run the script with a solution file:

```bash
python3 Test.py -f path/to/solution.py
```
